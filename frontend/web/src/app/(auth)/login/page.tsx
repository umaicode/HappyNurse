/**
 * 웹_로그인 화면.
 * 병원코드 · 아이디 · 비밀번호 + 직급 선택.
 * → features/auth/components/Form.tsx 사용.
 */
import { Form } from '@/features/auth/components/Form'

export default function LoginPage() {
  return <Form />
}
